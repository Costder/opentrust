import { LaunchLab } from "@/components/LaunchLab";

export const metadata = {
  title: "OpenTrust Launch Lab",
  description: "Interactive production-readiness lab for OpenTrust registry launch gates",
};

export default function LaunchLabPage() {
  return <LaunchLab />;
}
