# Passport Spec

Agent Tool Passports describe tool identity, creator identity, version/hash, capabilities, permissions, risk summary, review history, commercial status, billing plan, fee schedule, and agent access.

JSON Schema in `passport-schema/passport.schema.json` is the source of truth.
