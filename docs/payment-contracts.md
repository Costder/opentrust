# Payment Contracts

Implement `PaymentGateway`, `VerificationPricing`, `EscrowProvider`, and `SubscriptionManager` in `opentrust-private`.

Public API routes intentionally return 501 until a configured provider is installed.
