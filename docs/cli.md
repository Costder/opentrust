# CLI

`opentrust inspect`, `search`, `status`, `validate`, `claim`, `badge`, and `payment create-checkout` are provided.

Payment commands (`opentrust payment create-checkout`) are stubs. The reference registry does not process payments. Registry operators implement the payment endpoints against the OpenTrust schema.
