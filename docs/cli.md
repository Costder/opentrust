# CLI

`opentrust inspect`, `search`, `status`, `validate`, `claim`, `badge`, and `payment create-checkout` are provided.

Payment commands (`opentrust payment create-checkout`) create mock checkout records for local demos.
