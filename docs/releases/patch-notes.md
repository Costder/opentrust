# OpenTrust Patch Notes

Patch notes are for small changes that matter operationally but do not need a full release narrative.

## Unreleased

- Split the root README into a concise introduction and moved deeper project, release, and Hands Body and Feet material into `docs/`.
- Added `docs/README.md` as the documentation map.
- Added `docs/project/status.md` and `docs/project/roadmap.md`.
- Added release documentation under `docs/releases/`.

## Known Follow-Up

- Dependency audit findings in the current npm dependency graph need separate triage.
- Release docs should be updated as part of each tagged release, not only after large implementation pushes.
