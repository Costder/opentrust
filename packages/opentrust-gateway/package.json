{
  "name": "@infinitestudios/opentrust-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "OpenTrust MCP/API gateway runtime for hosted, remote, credential-backed, and local connector tools",
  "type": "module",
  "scripts": {
    "build": "tsup src/policy.ts --format esm --dts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/policy.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0",
    "supertest": "^7.0.0",
    "tsup": "^8.3.0",
    "tsx": "^4.20.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
