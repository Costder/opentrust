[pytest]
asyncio_mode = auto
# Add the repo root (rootdir) to sys.path so `api.src.*` imports resolve under a
# bare `pytest` invocation (CI), not just `python -m pytest` which adds CWD itself.
pythonpath = .
