# Passport Schema

JSON Schema is the source of truth for OpenTrust Agent Tool Passports. API models, CLI validation, and frontend types are aligned to these schemas.

Trust status must always be shown prominently. Auto-generated draft passports require the warning text included in `examples/minimal-passport.json`.
